<% if @comment.errors.any? %>
  Materialize.toast("<%= j @comment.errors.full_messages.join(', ') %>", 4000);
<% else %>
  $("#post_<%=@post.id%> .collection").append("<%= j render 'users/comment_element', {post: @post, comment: @comment}%>");
<% end %>